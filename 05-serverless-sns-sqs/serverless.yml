service: client

frameworkVersion: '2'

provider:
  name: aws
  runtime: nodejs12.x
  profile: jonnyalexbh
  region: ${opt:region, 'us-east-1'}
  stage: ${opt:stage, 'develop'}
  lambdaHashingVersion: 20201221

custom:
  stackPrefix: ${self:service}-sls-${self:provider.stage}
  clientTableName: ${self:service}-table-${self:provider.stage}

functions:
  - ${file(src/health_check/index.yml)}

resources:
  - ${file(resources/tables/clients.yml)}
